#include "main.h"
/**
 * _isupper -To check if upper
 * @c: character to print
 *  Return: On succes 1
 *  on error 0
 */

int _isupper(int c);
{
if (c >= 'A' && c <= 'Z')

	return (1);
else
	return (0);
}
